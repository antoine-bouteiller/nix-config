1. **Do not make assumptions:** Never start an implementation if anything is unclear. If you need more information, always ask the user for clarification first.
2. **Browser interactions:** When you need to interact with a browser or verify that a web page works correctly, you must use the `agent-browser` CLI.
3. **Code quality:** Always run lint and format after finishing a task.
